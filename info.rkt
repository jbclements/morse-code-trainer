#lang setup/infotab

(define collection 'multi)

(define deps
  (list "base"
        "math-lib"
        "rsound"
        "typed-racket-lib"
        "levenshtein"))

(define build-deps
  (list "rackunit-lib"))

